#local setup files in vscode
.vscode/
pyrightconfig.json

# runtime SQLite database (recreated by init_db on backend startup)
implementation/backend/data/*.db

# secrets (see implementation/.env.example for the template)
implementation/.env
implementation/backend/.env
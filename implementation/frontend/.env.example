# Frontend -> backend API base URL
VITE_API_URL=/api
# Optional dev-only Vite proxy target.
# Local default is http://localhost:8000, Docker compose overrides this to http://backend:8000.
# VITE_DEV_PROXY_TARGET=http://localhost:8000
